# Submission

**Challenge attempted:** #1 — Risk Radar (also delivers #3 — Status Summarizer
as part of the same workflow).

---

### 1. Prototype — public link
**Live app:** _add your Streamlit URL here_

Runs immediately in the browser, no login. It works on a free built-in engine
out of the box; AI analysis can be toggled on.

### 2. Public dataset
[`data/project_updates.csv`](data/project_updates.csv) — synthetic project
updates across 12 projects over ~10 weeks.

> Illustrative mock data, hand-generated for this demo — not real Careem data,
> and not affiliated with or sourced from Careem.

### 3. 100-word summary
See [`SUMMARY.md`](SUMMARY.md).

---

### Suggested 30-second walk-through for a reviewer
1. **Overview** — projects ranked worst-first; click a red one to expand and see
   *why* the AI flagged each issue.
2. **Project view** — the raw update shown next to the AI's read of it, plus a
   trend line and a status digest.
3. **How it works** — the workflow, and the AI's measured accuracy against
   hidden ground-truth labels in the data.

### Screenshots
_Optional: drop 2–3 images here (Overview, Project view) if submitting as a
document rather than a live link._
